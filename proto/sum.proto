syntax = "proto3";

package sum;

option go_package = "grpc-bidi-addition/pkg/proto;proto";

service SumService {
  rpc AddNumbers(stream NumberRequest) returns (stream NumberResponse);
}

message NumberRequest {
  int32 number = 1;
}

message NumberResponse {
  int32 total = 1;
}